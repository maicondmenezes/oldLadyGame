import React from 'react'
import Square from '../Square'

renderSquare(i) {    
  return(
    <Square playerSymbol = {i}/>
  );
}

export default props => (
  
  render (){
    return
  }

)
export default Board